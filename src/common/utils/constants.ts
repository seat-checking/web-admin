export const PATH = {
  shopStatus: 'shopstatus',
  layout: 'layout',
  statistics: 'statistics',
  setting: 'setting',
  login: 'login',
  join: 'join',
} as const;

export const STORAGE = {
  accessToken: 'accessToken',
} as const;
